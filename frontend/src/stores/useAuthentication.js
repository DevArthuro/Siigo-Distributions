import { defineStore } from 'pinia'

const useAuthentication = defineStore({
    state: () => ({
        token: '',
    }),
    actions: {
        // User to logger
        userRegister(fist_name, last_name, email, password)
        {
            const response = async 
        }
    },
    persist: true,
})